import React from 'react';

const StepPhoneEmail = ({ onNext }) => {
    return (
        <>
            <div>EmailPhone component</div>
            <button onClick={onNext}>Next</button>
        </>
    );
};

export default StepPhoneEmail;